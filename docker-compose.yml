
version: '3'

services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment: 
      - POSTGRES_PASSWORD=P@ssword1!
      - POSTGRES_USER=sebaraba
      - POSTGRES_DB=nodeApplicationDB
  # web:
  #   build:
  #     context: ./client
  #     dockerfile: Dockerfile
  #   volumes:
  #     - /app/node_modules
  #     - ./client:/app
  #   ports:
  #     - '3000:3000'
  #   restart: on-failure
  #   container_name: web
  #   tty: true
  api:
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    ports:
      - '5001:5000'
    restart: on-failure
    container_name: api
    volumes:
      - /app/node_modules # Inside the container, don't try to override this folder, just leave as is
      - ./server:/app # Look at the server directory and copy everything into the app folder in the container
    environment:
      - PGUSER=sebaraba
      - PGHOST=localhost
      - PGDATABASE=nodeApplicationDB
      - PGPASSWORD=P@ssword1!
      - PGPORT=5432
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile.dev
    ports:
      - 80:80
    container_name: nginx